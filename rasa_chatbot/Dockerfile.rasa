FROM rasa/rasa-sdk:3.6.0

WORKDIR /app

COPY actions/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY actions/ ./actions/

EXPOSE 5055

CMD ["start", "--actions", "actions.actions", "--port", "5055"]
